#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: the integer to compute the absolute value of
 *
 * Return: 0 or 1
 */
int _abs(int n)
{
	return (n >= 0 ? n : n * -1);
}
